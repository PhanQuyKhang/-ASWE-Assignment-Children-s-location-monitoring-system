# Base URL of the CLMS Express API (same origin used by axios + Socket.IO)
VITE_API_URL=http://localhost:3000

# OneSignal Web push — Settings → Keys & IDs (matches dashboard Site URL to your deployed origin)
VITE_ONESIGNAL_APP_ID=
